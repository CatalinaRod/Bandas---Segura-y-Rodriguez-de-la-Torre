<!DOCTYPE html>
<html lang="en">
<body>
    <div>
        
        <% if (listado.length == 0) { %>
            <p>No hay datos disponibles.</p>
        <% } else { %>
            <h1>Genero:</h1>
            <% for (let i = 0; i < listado.length; i++) { %>
                <ul>
                    <li><a href="/banda/id/<%= listado[i].id %>"> <%= listado[i].genero %>:<%= listado[i].nombre %></a></li>
                </ul>
            <% } %>
        <% } %>
        
    </div>
</body>
</html>
